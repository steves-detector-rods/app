import DefaultHead from '../default-head';

export default function Head() {
	return (
		<head>
			<DefaultHead />
			<title>Steve&apos;s Detector Rods | Products</title>
			<meta
				name="description"
				content="Shop all of the latest metal detector shafts, carbon-fiber rods, sand scoop handles and more!"
			/>
		</head>
	);
}
